retargeting:
  type: vector
  urdf_path: "inspire_hand_viz.urdf"
  wrist_link_name: "R_hand_base_link"
  low_pass_alpha: 0.2
  scaling_factor: 1.0

  # All independent (non-mimic) joints for the right hand
  target_joint_names:
    - "R_thumb_proximal_yaw_joint"
    - "R_thumb_proximal_pitch_joint"
    - "R_index_proximal_joint"
    - "R_middle_proximal_joint"
    - "R_ring_proximal_joint"
    - "R_pinky_proximal_joint"

  # Vector retargeting: defines pairs of (origin, task) links on the robot.
  # Each pair forms a direction vector that is matched to the corresponding
  # human hand direction vector defined by target_link_human_indices.

  # Origin links (start of each vector on the robot)
  target_origin_link_names:
    - "R_hand_base_link"    # Thumb vector origin
    - "R_hand_base_link"    # Index vector origin
    - "R_hand_base_link"    # Middle vector origin
    - "R_hand_base_link"    # Ring vector origin
    - "R_hand_base_link"    # Pinky vector origin

  # Task links (end of each vector on the robot — fingertips)
  target_task_link_names:
    - "R_thumb_distal"       # Thumb tip
    - "R_index_intermediate" # Index tip
    - "R_middle_intermediate"# Middle tip
    - "R_ring_intermediate"  # Ring tip
    - "R_pinky_intermediate" # Pinky tip

  # MediaPipe landmark indices for human hand vectors
  # Shape must be (2, N): row 0 = origin indices, row 1 = task indices
  # 0=Wrist, 4=Thumb tip, 8=Index tip, 12=Middle tip, 16=Ring tip, 20=Pinky tip
  target_link_human_indices:
    - [0, 0, 0, 0, 0]      # Origins: all Wrist
    - [4, 8, 12, 16, 20]   # Tasks: Thumb, Index, Middle, Ring, Pinky tips
